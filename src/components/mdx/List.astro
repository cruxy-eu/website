---
interface Props {
  ordered?: boolean;
  class?: string;
}

const { ordered = false, class: className, ...attrs } = Astro.props;
const Tag = ordered ? 'ol' : 'ul';
---

<Tag class:list={["space-y-4 list-none max-w-4xl mx-auto", className]} {...attrs}>
  <slot />
</Tag>
