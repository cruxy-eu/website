---
interface Props {
  ordered?: boolean;
  class?: string;
}

const { ordered = false, class: className, ...attrs } = Astro.props;
const Tag = ordered ? 'ol' : 'ul';
---

<Tag class:list={["space-y-2 list-none", className]} {...attrs}>
  <slot />
</Tag>
